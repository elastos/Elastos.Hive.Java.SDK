network = MainNet
resolver = http://api.elastos.io:20606

# network = TestNet
# resolver = http://api.elastos.io:21606

dummystore.verbose = true

mnemnoic.passphrase = secret

temp.dir = TEMP

store.root = DIDStore
store.pass = passwd

wallet.dir = Wallet
wallet.id = test
wallet.password = passwd
